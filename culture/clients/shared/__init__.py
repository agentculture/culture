"""Shared helpers used by every culture client backend.

Place culture-side utilities that don't fit cleanly into a single backend
here. Anything imported from `agentirc.*` should be re-exported (with a
clear docstring) rather than reached into directly from each backend.
"""
