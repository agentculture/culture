"""Developer tooling shipped with the engine (CI guards and parity checks).

These modules are stdlib-only helpers that back repository automation — they
run in CI (and locally) against the git checkout, not against a live mesh.
"""
