name: Publish to PyPI

on:
  push:
    branches: [main]
    paths:
      - "pyproject.toml"
      - "culture/**"
  pull_request:
    branches: [main]
    paths:
      - "pyproject.toml"
      - "culture/**"

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v4

      - run: uv python install 3.12

      - run: uv sync

      - run: uv run pytest -v

  test-publish:
    if: github.event_name == 'pull_request'
    needs: test
    runs-on: ubuntu-latest
    environment: testpypi
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v4

      - run: uv python install 3.12

      - run: uv sync

      - name: Set dev version
        run: |
          BASE=$(python -c "import tomllib; print(tomllib.load(open('pyproject.toml','rb'))['project']['version'])")
          DEV_VERSION="${BASE}.dev${{ github.run_number }}"
          sed -i "s/^version = .*/version = \"${DEV_VERSION}\"/" pyproject.toml
          echo "DEV_VERSION=${DEV_VERSION}" >> "$GITHUB_ENV"
          echo "Publishing ${DEV_VERSION} to TestPyPI"

      - name: Build and publish culture to TestPyPI
        run: |
          uv build
          uv publish --publish-url https://test.pypi.org/legacy/ --trusted-publishing always --check-url https://test.pypi.org/simple/

      - name: Build and publish agentirc-cli alias to TestPyPI
        run: |
          sed -i 's/^name = "culture"/name = "agentirc-cli"/' pyproject.toml
          rm -rf dist
          uv build
          uv publish --publish-url https://test.pypi.org/legacy/ --trusted-publishing always --check-url https://test.pypi.org/simple/

      - name: Build and publish agentirc alias to TestPyPI
        run: |
          sed -i 's/^name = "agentirc-cli"/name = "agentirc"/' pyproject.toml
          rm -rf dist
          uv build
          uv publish --publish-url https://test.pypi.org/legacy/ --trusted-publishing always --check-url https://test.pypi.org/simple/

      - name: Print install commands
        run: |
          echo "::notice::Test with: pip install --index-url https://test.pypi.org/simple/ --extra-index-url https://pypi.org/simple/ culture==${DEV_VERSION}"
          echo "::notice::Or (legacy): pip install --index-url https://test.pypi.org/simple/ --extra-index-url https://pypi.org/simple/ agentirc-cli==${DEV_VERSION}"
          echo "::notice::Or (legacy): pip install --index-url https://test.pypi.org/simple/ --extra-index-url https://pypi.org/simple/ agentirc==${DEV_VERSION}"

  publish:
    if: github.event_name == 'push'
    needs: test
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v4

      - run: uv python install 3.12

      - run: uv sync

      - name: Build and publish culture to PyPI
        run: |
          uv build
          uv publish --trusted-publishing always --check-url https://pypi.org/simple/

      - name: Build and publish agentirc-cli alias to PyPI
        run: |
          sed -i 's/^name = "culture"/name = "agentirc-cli"/' pyproject.toml
          rm -rf dist
          uv build
          uv publish --trusted-publishing always --check-url https://pypi.org/simple/

      - name: Build and publish agentirc alias to PyPI
        run: |
          sed -i 's/^name = "agentirc-cli"/name = "agentirc"/' pyproject.toml
          rm -rf dist
          uv build
          uv publish --trusted-publishing always --check-url https://pypi.org/simple/
